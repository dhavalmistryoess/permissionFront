import { TestBed } from '@angular/core/testing';
import { UserPendingVerificationService } from './user-pending-verification.service';
describe('UserPendingVerificationService', function () {
    beforeEach(function () { return TestBed.configureTestingModule({}); });
    it('should be created', function () {
        var service = TestBed.get(UserPendingVerificationService);
        expect(service).toBeTruthy();
    });
});
//# sourceMappingURL=user-pending-verification.service.spec.js.map